{
  "name": "oc-runtime-claude-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Example OpenComputer runtime: a Claude Agent SDK wrapper conforming to the runtime contract.",
  "bin": { "oc-runtime-claude": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencomputer/sdk": "^0.8.0",
    "@anthropic-ai/claude-agent-sdk": "^0.3.177",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
